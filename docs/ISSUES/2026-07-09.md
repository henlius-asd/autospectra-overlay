# 2026-07-09 问题记录

## 索引

| # | 标题 | 类别 | 状态 | 优先级 |
|---|------|------|------|--------|
| 1 | 纵坐标范围选择（预览图框选） | 功能缺陷 | 待处理 | - |
| 2 | 标签字体大小调整 | 功能缺陷 | 待处理 | - |
| 3 | 导出可编辑图片 | 功能缺陷 | 待处理 | - |
| 4 | 图例圆点与线条颜色不一致 | 功能缺陷 | 待处理 | - |
| 5 | 曲线与坐标刻度轴需有间隔 | 交互体验 | 待处理 | - |
| 6 | 曲线手动横向/纵向移动（含锁定功能） | 功能缺陷 | 待处理 | - |
| 7 | 间隔滑块坍缩为点且无法调整（个别机器） | 功能缺陷 | 已完成 | - |

---

## 功能缺陷

### 1. 纵坐标范围选择（预览图框选）

**状态**：待处理

**优先级**：-

**变更链接**：

**问题提出**：黄萍提出纵坐标设置类似横坐标的横条，通过拉动横条选择范围用于放大图叠图。

**原因分析**：目前考虑做成预览图形式，在预览图中框选可视范围。两边都做滑条逻辑较复杂，计划做两个版本（预览图框选方案 vs 双侧滑条方案）比较操作便利性。

**解决方案**：开发预览图框选方案与双侧滑条方案，比较后选定更优方案。

---

### 2. 标签字体大小调整

**状态**：待处理

**优先级**：-

**变更链接**：

**问题提出**：黄萍提出标签里的字体大小设置需要可调整。

**原因分析**：当前标签字体大小固定，用户无法自定义。

**解决方案**：新增字体样式设置工具，支持用户调整标签字体大小。

---

### 3. 导出可编辑图片

**状态**：待处理

**优先级**：-

**变更链接**：

**问题提出**：黄萍提出需要导出可编辑图片。

**原因分析**：相对困难，当前导出为静态图片格式，无法满足后续编辑需求。

**解决方案**：探索将图片分图层导出至 PPT 的技术路径，使导出内容可在 PPT 中编辑。

---

### 4. 图例圆点与线条颜色不一致

**状态**：待处理

**优先级**：-

**变更链接**：

**问题提出**：张嫱提出左栏数据区曲线数据旁的原点对应线条颜色，与图例上的圆点和线条颜色不一致。询问能否统一把图例上的圆圈删掉，只体现线条颜色。

**原因分析**：历史遗留问题。目前导出的图片不会显示图例，接下来会对这部分进行调整。

**解决方案**：调整图例显示逻辑，统一颜色表示方式，考虑删除图例中的圆圈仅保留线条颜色。

---

### 6. 曲线手动横向/纵向移动（含锁定功能）

**状态**：待处理

**优先级**：-

**变更链接**：

**问题提出**：Jenny（邵珍珍）提出能否横向单个样左右移动（该功能可锁定，锁定后无法横向移动），纵向单个样调整间距。

**原因分析**：当前横向移动依赖自动对齐功能完成，缺少手动微调能力。需要在自动对齐基础上增加手动调整功能，并支持锁定横向位置防止误操作。

**解决方案**：实现曲线的手动横向和纵向移动功能，增加锁定机制控制横向移动权限。

---

### 7. 间隔滑块坍缩为点且无法调整（个别机器）

**状态**：已完成

**优先级**：-

**变更链接**：[fix-layer-spacing-slider-collapse](../../openspec/changes/archive/2026-07-09-fix-layer-spacing-slider-collapse/)

**问题提出**：肖帅报告在个别机器上，间隔滑块坍缩成一个点而不是竖条，且无法改变值，间隔固定为默认最大值 0.5。

**原因分析**：滑块包裹层（`absolute top-1/2 right-1 -translate-y-1/2`，仅设 `top` 无 `height`）高度为 auto，导致其上的 `<input class="layer-slider h-3/5">` 百分比高度被浏览器忽略，回退到内在高度 14px（仅 thumb），轨道塌缩为 0px。经复现测试确认：Bug 版 input 高度 14px，对照组（父级显式高度）180px。

**解决方案**：将百分比高度 `h-3/5` 从 `<input>` 移到包裹层 div（absolutely positioned，百分比解析到确定高度的定位祖先），input 改用 `flex-1` 填充剩余空间。修复后 input 高度 253.2px，可正常拖动。

---

## 交互体验

### 5. 曲线与坐标刻度轴需有间隔

**状态**：待处理

**优先级**：-

**变更链接**：

**问题提出**：张嫱提出曲线与坐标刻度轴需有间隔，不要贴线。于璐之前也提出了相同的问题。

**原因分析**：当前可以隐藏坐标轴，但 X 轴坐标轴位置默认设定为 y=0，导致底层曲线紧贴坐标轴。

**解决方案**：调整 X 轴坐标轴位置，不默认设定为 y=0，改为放在整体曲线下方。

---

## 性能/架构

（暂无）

## 其他

（暂无）
