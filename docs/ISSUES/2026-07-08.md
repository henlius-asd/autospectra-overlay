# 2026-07-08 问题记录

## 索引

| # | 标题 | 类别 | 状态 | 优先级 |
|---|------|------|------|--------|
| 1 | Empower 导出格式兼容 | 功能缺陷 | 已完成 | - |
| 2 | 独立 Y 轴缩放 | 功能缺陷 | 进行中 | - |
| 3 | 曲线颜色修改 / 调色板 | 功能缺陷 | 已完成 | - |
| 4 | 底层曲线与 X 轴间距 | 交互体验 | 待处理 | - |

---

## 功能缺陷

### 1. Empower 导出格式兼容

**状态**：已完成

**变更链接**：

**问题提出**：肖帅提出原始数据无法导入。谷艺豪复查后发现是 Empower 导出格式因设置原因与其他人的格式不一致，导致解析失败。

**原因分析**：（推测）Empower 软件在导出时可能存在不同的格式设置选项，不同用户的导出设置不同，导致输出的 `.arw` 文件格式存在差异，现有解析器未覆盖该变体格式。`待确认`

**解决方案**：针对该格式变体实现了解析逻辑，将数据转换为可解析的版本后完成导入。

---

### 2. 独立 Y 轴缩放

**状态**：进行中

**优先级**：-

**变更链接**：

**问题提出**：张嫱提出叠图不能放大纵轴，只能调整曲线间距；耿辉亮希望每条曲线有独立的 Y 轴缩放，同一张图上不同曲线可有不同的缩放比例。

**原因分析**：

**解决方案**：

---

### 3. 曲线颜色修改 / 调色板

**状态**：已完成

**优先级**：-

**变更链接**：

**问题提出**：张嫱、于璐提出需要修改曲线颜色，当前所有曲线使用固定默认配色，用户无法自定义。

**原因分析**：

**解决方案**：

---

## 交互体验

### 4. 底层曲线与 X 轴间距

**状态**：待处理

**优先级**：-

**变更链接**：

**问题提出**：于璐提出最底层曲线可以与 X 轴拉开距离，避免曲线紧贴坐标轴，影响视觉区分。

**原因分析**：

**解决方案**：

---

## 性能/架构

（暂无）

## 其他

（暂无）